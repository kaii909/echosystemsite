{
  "name": "vite-project",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "preview": "vite preview",
    "dev:templ": "go tool templ generate --watch",
    "dev:go": "go run ./main.go",
    "dev:vite": "vite",
    "dev": "concurrently -k -n GO,TEMPL,VITE -c blue,magenta,green \"npm run dev:go\" \"npm run dev:templ\" \"npm run dev:vite\""
  },
  "devDependencies": {
    "concurrently": "^10.0.5",
    "vite": "^8.2.2"
  },
  "dependencies": {
    "htmx.org": "^2.0.10"
  }
}
